import 'package:flutter/material.dart';

class FavoriteesScreen extends StatelessWidget {
  const FavoriteesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Favorites"),
      ),
    );
  }
}
